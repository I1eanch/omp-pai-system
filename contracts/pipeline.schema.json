{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:omp-pai-system:schema:pipeline:1",
  "title": "OMP PAI Pipeline definition",
  "description": "Ordered deterministic Action pipeline. Runtime validation additionally rejects duplicate step ids, forward references, malformed references, and references to missing output paths.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "steps"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "id": { "$ref": "#/$defs/id" },
    "steps": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1000,
      "items": { "$ref": "#/$defs/step" }
    }
  },
  "$defs": {
    "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,63}$" },
    "step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "action", "input"],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "action": { "$ref": "#/$defs/id" },
        "input": {
          "description": "Any JSON value. Strings beginning with $ are pipeline references and are validated against preceding steps by the runtime."
        }
      }
    }
  }
}
